# StepStitch ingest host (deploy target). The stepstitch-service package itself is
# installed from ./service in the Dockerfile (pip install ./service).
fastapi>=0.104
uvicorn[standard]>=0.30
asyncpg>=0.29
# Operator SSO: RS256 JWT verification against the OIDC issuer's JWKS.
PyJWT[crypto]>=2.8
# Schema migrations. Alembic runs over a sync SQLAlchemy/psycopg2 engine at
# deploy/startup only; the runtime trace path stays on asyncpg.
alembic>=1.13
# psycopg2 is the sync driver Alembic/SQLAlchemy use for migrations ONLY
# (runtime DB access uses asyncpg, not this).
psycopg2-binary>=2.9
